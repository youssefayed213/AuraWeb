<form method="post" action="{{ path('app_terrain_delete', {'id': terrain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terrain.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
