<link rel="stylesheet" href="{{asset('back/css/styleTech.css')}}" type="text/css"> 
<form method="post" action="{{ path('app_affectations_delete', {'id': affectation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectation.id) }}">
    <button class="btn">Delete</button>
</form>
