{% extends 'base.html.twig' %}

{% block title %}FraisEnergie{% endblock %}

{% block body %}
    <h1>FraisEnergie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frais_energie.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ frais_energie.montant }}</td>
            </tr>
            <tr>
                <th>Taux_Energie</th>
                <td>{{ frais_energie.tauxEnergie }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ frais_energie.date ? frais_energie.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frais_energie_index') }}">back to list</a>

    <a href="{{ path('app_frais_energie_edit', {'id': frais_energie.id}) }}">edit</a>

    {{ include('frais_energie/_delete_form.html.twig') }}
{% endblock %}
