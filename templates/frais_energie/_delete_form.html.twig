<form method="post" action="{{ path('app_frais_energie_delete', {'id': frais_energie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_energie.id) }}">
    <button class="btn">Delete</button>
</form>
